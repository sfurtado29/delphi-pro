version: "3.9"

services:
  backend:
    build: ./backend
    container_name: delphi-backend
    env_file: ./backend/.env
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: delphi-frontend
    ports:
      - "3000:80"